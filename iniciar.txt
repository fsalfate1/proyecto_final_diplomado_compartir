python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
python persistencia_neon_sencillo.py

# En otra terminal (opcional, para acceso remoto):
ngrok http 8001
# Abre la URL https://...ngrok-free.dev
